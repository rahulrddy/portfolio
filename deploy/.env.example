# Optional: latest follows successful publications from master.
# Use a full published commit SHA to pin or roll back a release.
IMAGE_TAG=latest

# The existing shared Cloudflare gateway's Docker network.
PROXY_NETWORK=web
